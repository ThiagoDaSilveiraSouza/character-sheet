import { SpellPageHook } from './SpellPageHook';

export const useSpellPage = () => {
  const hookResult = SpellPageHook();

  // Adicione aqui qualquer lógica adicional específica para o componente SpellPage

  return hookResult;
};
